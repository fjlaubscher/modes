import React from 'react';

import { StyledNav, AppIcon, Heading } from './style';

const Nav = () => (
  <StyledNav>
    <AppIcon />
    <Heading>Modes</Heading>
  </StyledNav>
);

export default Nav;
